#include"bits/stdc++.h"

using namespace std;

int main()
{
    int g, c, l;
    cin >> g >> c >> l;
    int x=max(g, max(c, l));
    int y=min(g, min(c, l));
    if((x-y)>=10) cout << "check again" << endl;
    else cout << "final " << g+c+l-x-y << endl;
    return 0;
}
