#include"bits/stdc++.h"

using namespace std;

int main()
{
    int n, cnt=0;
    cin >> n;
    int arr[n];
    for(int i=0; i<n; i++)
        cin >> arr[i];
    int mx=*max_element(arr, arr+n);
    for(int i=0; i<n; i++)
        cnt+=mx-arr[i];
    cout << cnt << endl;
    return 0;
}
